# 产品管理（后台上传真实产品图与资料）

网站前台的 **Catalogue（产品目录）** 现在由后台真实数据驱动。你无需再改动代码，直接通过管理后台上传产品图片和文字即可展示在官网。

## 访问后台

- 网址：`https://你的域名/admin`（当前临时地址为 `https://<随机>.trycloudflare.com/admin`）
- 账号：`admin`
- 密码：`helenthy`（生产环境请在服务器 `.env` 的 `ADMIN_CREDENTIALS` 中修改）

## 上传一个产品

1. 登录后，点击顶部第二个标签 **「产品管理」**。
2. 点击右上角 **「+ 新建产品」**，填写：
   - **产品名称**（必填，英文展示给海外客户）
   - **分类**：Awards / Candle Holders / Photo Frames / Ornaments 四选一
   - **描述**：一段英文介绍
   - **规格参数**：每行一条 `参数名: 值`，例如
     ```
     Material: K9 optical crystal
     Size: H 20 cm
     MOQ: 100 pcs
     Lead time: 15-25 days
     ```
   - **排序**：数字越小越靠前
   - **产品图片**：点击选择本地图片（PNG/JPG/WebP，≤ 8 MB）
3. 点 **「保存」**。前台目录会自动刷新显示该产品与真实图片。

## 修改 / 删除

- 在「产品管理」列表中，每个产品右侧有 **编辑** 与 **删除** 按钮。
- 重新上传图片会**自动替换并删除旧图**，不会堆积垃圾文件。
- 删除产品会同时删除其图片文件。

## 注意事项

- 图片通过 Cloudflare 隧道传输；已做优化（显式 `Content-Length`），正常可显示。若图片偶尔加载慢，属隧道带宽限制，属正常现象。
- 建议单图控制在 500 KB 以内，上传更快、客户打开更顺畅。
- 若后台为空（尚未上传任何产品），前台会回退显示一套示例占位产品，不会空白。

## 数据存放在哪里

- 产品与图片保存在服务器上：`data/`（SQLite 数据库）和 `uploads/`（图片文件）。
- 备份时请同时备份这两个目录（见 `scripts/backup.sh`）。
